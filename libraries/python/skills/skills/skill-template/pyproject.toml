[tool.poetry]
name = "your-skill"
version = "0.1.0"
description = "MADE:Exploration skill"
authors = ["MADE:Explorers"]
readme = "README.md"

[tool.poetry.dependencies]
python = "~3.11"
skill-library = { path = "../../skill-library", develop = true }
chat-driver = { path = "../../../chat-driver", develop = true }
context = { path = "../../../context", develop = true }
events = { path = "../../../events", develop = true }

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.1"
pytest-asyncio = "^0.23.8"
pytest-repeat = "^0.9.3"
ruff = "^0.6.4"

[build-system]
requires = ["poetry-core>=1.2.0"]
build-backend = "poetry.core.masonry.api"

[tool.pyright]
exclude = ["venv", ".venv"]
